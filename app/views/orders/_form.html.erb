<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <form>
      <div class="form-group row">
        <%= f.label :order_date, class: "col-2 col-form-label text-green" %>
        <div class="col-10">
          <%= f.text_field :order_date, class: "form-control shadow rounded", placeholder: "Order Date" %>
        </div>
      </div>
      <div class="form-group row">
        <%= f.label :user_id, class: "col-2 col-form-label text-green" %>
        <div class="col-10"> 
          <%= f.text_area :user_id, rows: 10, class: "form-control shadow rounded", placeholder: "User_id" %>
        </div>
      </div>
      <div class="form-group row">
        <%= f.label :status, class: "col-2 col-form-label text-green" %>
        <div class="col-10"> 
          <%= f.text_field :status, class: "form-control shadow rounded", placeholder: "Status" %>
        </div>
      </div>
      
          
          <div class="col-10">
        <%= f.submit class: "btn btn-outline-success btn btn-primary btn-lg btn-block" %>
        </div>
        </div>
      </div>
    </form>
<% end %>

   
