<%= form_for(provenience) do |f| %>
  <% if provenience.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(provenience.errors.count, "error") %> prohibited this provenience from being saved:</h2>

      <ul>
      <% provenience.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
     <form>
      <div class="form-group row">
        <%= f.label :title, class: "col-2 col-form-label text-green" %>
        <div class="col-10">
          <%= f.text_field :title, class: "form-control shadow rounded", placeholder: "Title" %>
        </div>
      </div>
      <div class="form-group row">
        <%= f.label :description, class: "col-2 col-form-label text-green" %>
        <div class="col-10"> 
          <%= f.text_area :description, rows: 10, class: "form-control shadow rounded", placeholder: "Description" %>
        </div>
      </div>
      <div class="form-group row">
        <%= f.label :image, class: "col-2 col-form-label text-green" %>
        <div class="col-10"> 
          <%= f.text_field :image, class: "form-control shadow rounded", placeholder: "Image url" %>
        </div>
      </div>
      
          
          <div class="col-10">
        <%= f.submit class: "btn btn-outline-success btn btn-primary btn-lg btn-block" %>
        </div>
        </div>
      </div>
    </form>
  
  
  
  

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

<% end %>

<%= render 'items/footer' %> 
